/**
 * MenuButtonProps is an interface for the props of the MenuButton component.
 *
 * @property {() => void} onClick - The function to call when the button is clicked.
 */
interface MenuButtonProps {
  onClick: () => void;
}
