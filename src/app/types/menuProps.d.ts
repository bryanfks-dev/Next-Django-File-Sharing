/**
 * MenuProps is an interface that defines the properties of
 * the Menu component.
 *
 * @property {string} label - the label of the menu item
 * @property {string} href - the href of the menu item
 */
interface MenuProps {
  label: string;
  href: string;
}
